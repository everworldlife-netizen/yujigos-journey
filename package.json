{
  "name": "yujigos-journey",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate-assets && tsc --noEmit && vite build",
    "preview": "vite preview",
    "setup": "node scripts/download-assets.mjs",
    "generate-assets": "node scripts/generate-assets.js"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "node-fetch": "^3.3.2",
    "canvas": "^3.1.2"
  }
}
